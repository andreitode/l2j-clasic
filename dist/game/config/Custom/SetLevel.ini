# ---------------------------------------------------------------------------
# SETLEVEL 
# ---------------------------------------------------------------------------

# If you set in true all players can type .setlevel lvl to change lvl automatly (Example: .setlevel 78).
# WARRNING: Only for test server
# Default false

EnableSetLevelCommand = true